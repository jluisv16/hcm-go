apiVersion: v1
kind: Service
metadata:
  name: hcm-go
  labels:
    app: hcm-go
spec:
  selector:
    app: hcm-go
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
